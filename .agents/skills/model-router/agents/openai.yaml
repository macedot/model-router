interface:
  display_name: "Model Router"
  short_description: "Repo-specific patterns and workflows for model-router"
  default_prompt: "Use the model-router repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true